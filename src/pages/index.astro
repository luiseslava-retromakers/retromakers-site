---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import ArticleCard from '../components/ArticleCard.astro';
import NewsletterForm from '../components/NewsletterForm.astro';

const posts = (await getCollection('blog', ({ data }) => !data.draft))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 6);
---

<BaseLayout title="Inicio">
  <section class="hero">
    <h1>Datos curiosos e historias de los juegos con los que creciste.</h1>
    <p>Trivia de los 80s, 90s y 2000s — las historias detrás de los juegos, contadas de forma divertida.</p>
  </section>

  <section>
    <h2>Últimas historias</h2>
    <div class="grid">
      {posts.map((post) => (
        <ArticleCard slug={post.id} title={post.data.title} description={post.data.description} decades={post.data.decades} />
      ))}
    </div>
  </section>

  <section>
    <NewsletterForm />
  </section>
</BaseLayout>

<style>
  .hero {
    text-align: center;
    padding: 3rem 0 2rem;
  }
  .hero h1 {
    font-size: 2.2rem;
  }
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: 1.25rem;
    margin: 1.5rem 0 2.5rem;
  }
</style>

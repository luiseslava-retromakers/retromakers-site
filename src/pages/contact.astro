---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Contacto">
  <article class="prose-page">
    <h1>Contacto</h1>
    <p>¿Tienes un dato curioso, una corrección, o un juego que crees que merece su propia página? Escríbenos.</p>

    <!--
      Netlify Forms: this works automatically once deployed on Netlify — no backend needed.
      The data-netlify="true" attribute is what triggers Netlify's form handling at build time.
    -->
    <form name="contact" method="POST" data-netlify="true">
      <input type="hidden" name="form-name" value="contact" />
      <label>
        Nombre
        <input type="text" name="name" required />
      </label>
      <label>
        Correo electrónico
        <input type="email" name="email" required />
      </label>
      <label>
        Mensaje
        <textarea name="message" rows="5" required></textarea>
      </label>
      <button type="submit" class="button">Enviar</button>
    </form>

    <p>O escríbenos directo a <a href="mailto:leslava@retromakers.com">leslava@retromakers.com</a>.</p>
  </article>
</BaseLayout>

<style>
  .prose-page {
    max-width: 560px;
    margin: 2rem auto;
  }
  form {
    display: flex;
    flex-direction: column;
    gap: 0.9rem;
    margin: 1.5rem 0;
  }
  label {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
    font-weight: 500;
  }
  input,
  textarea {
    padding: 0.6rem;
    border-radius: var(--radius);
    border: 1px solid #ddd;
    font-family: var(--font-body);
  }
</style>
